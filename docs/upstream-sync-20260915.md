# 上游同步审查：2026-09-15

本次对照用户主项目 `2af74d8b3` 与 `Wei-Shaw/sub2api` 的
`bdb42e22f81fcb633ff0a060961211dd2bcb515b`。最新正式版为
[v0.2.4](https://github.com/Wei-Shaw/sub2api/releases/tag/v0.2.4)。

上游 main 已经通过 `cc02a0484` 合入当前历史，`origin/main..upstream/main`
没有待合入提交，实际 merge 返回 `Already up to date`。审查同时比较文件内容，
防止后续定制修改覆盖已经进入历史的上游修复。

## 上游近期变化

以下是 v0.2.4 发布后上游 main 的主要变化，当前代码已包含：

- OpenCode Zen/Go 接入及三种协议的模型映射。
- OpenAI WS 常驻读循环、执行作用域隔离、容量计算和等待者唤醒。
- 最大上下文窗口、Fast 服务档位、隐私检查指纹和原生 Codex Images 改进。
- Gemini 3.7/3.8 Flash、DeepSeek V4.1 Flash 计价和跨平台 Token 请求统计。
- 站点计费模式、模型目录检索、临时认证故障保留会话。
- 代理部分导入刷新、筛选分页、用量导出筛选快照等修复。

## 保留的定制能力

保持流式响应保护、短回合检测与重试、429 重试策略、主副账号恢复和 LRU、
后台流探针、派生价格倍率与长上下文阶梯、凭证回填、运行模式缓存，
以及现代代理、原生 sing-box 配置和订阅备份功能。

更新下载仍指向用户自己的仓库。程序版本号与上游 VERSION 一致，定制构建由
Git 提交号和镜像 revision 区分。

## Spec

发现一项已合入修复的内容回退：上游 `5bafffed5` / #7053 在协议或状态筛选变化时
重置代理页码，定制页面重新绑定为直接加载后丢失了该行为。

现恢复上游处理方式，并覆盖定制的订阅源筛选。三种筛选均先回第一页再请求数据，
保留现代代理页面和订阅管理功能。回归测试从第三页分别操作三种筛选。

部分导入刷新、上下文窗口、Fast 档位、认证临时失败处理、输出配置 beta，以及
WS 容量和唤醒逻辑均核对保留。此轴发现 1 项 P2。

## Standards

发现派生定价与独立 1h 缓存价格字段的交互问题：模型广场与可用渠道只派生了
默认缓存写入价，1h 展示仍可能使用目录价格，与实际扣费不同。

现让未显式配置的 1h 展示价格采用相同派生值；显式 1h 价格（包括 0）优先。
修复只调整展示价格，保持现有计费和配置语义，不修改缓存中的原始配置。
回归覆盖渠道输入价、目录输入价及显式 1h 价格。此轴发现 1 项 P2。

工程轴与规格轴各发现 1 项 P2，均通过局部修复保留原有定制行为。
